[project]
name = "sunner-line"
version = "0.1.0"
description = "Sunner poultry processing-line telemetry simulator and lightweight API bridge"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
